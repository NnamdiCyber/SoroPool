apiVersion: v1
kind: Service
metadata:
  name: indexer
  namespace: soropool-prod
  labels:
    app: indexer
    tier: worker
spec:
  type: ClusterIP
  ports:
    - name: metrics
      port: 3002
      targetPort: 3002
      protocol: TCP
  selector:
    app: indexer
